# Test Plan (Initial)

This folder will host parser and calculator tests.

Planned first tests:

1. Parse simple formulas (`H2O`, `CO2`).
2. Parse grouped formulas (`Ca(OH)2`).
3. Reject invalid formulas (`2H`, `Na(Cl`, `Xx2`).
4. Validate molar mass outputs for a small known set.
